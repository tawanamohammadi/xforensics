# 🕵️‍♂️ X Profile Forensics (ابزار تحلیلگر پروفایل ایکس)

**نسخه ۱۵.۴ (Version 15.4.0)**

این یک ابزار پیشرفته **OSINT** (جستجوی اطلاعات آشکار) برای شبکه اجتماعی **ایکس (توییتر سابق)** است. این اسکریپت اطلاعات فنی و مخفی حساب‌های کاربری را از دل APIهای توییتر استخراج کرده و به صورت **کاملاً یکپارچه (Native)** در رابط کاربری نمایش می‌دهد.

این ابزار برای **کامپیوتر (ویندوز/مک/لینوکس)** و **موبایل (اندروید/iOS)** بهینه شده است.

---

## ✨ ویژگی‌های جدید و کلیدی (Key Features)

### ۱. 🎨 رابط کاربری بومی (Native UI Integration)
دکمه‌های مزاحم و شناور حذف شدند! این ابزار اکنون بخشی از خودِ توییتر به نظر می‌رسد:
*   **🖥️ در کامپیوتر:** گزینه **Forensics** به **منوی اصلی سمت راست** (کنار Home و Profile) اضافه شده است.
*   **📱 در موبایل:** آیکون **پایگاه داده (📂)** به **نوار ابزار پایین صفحه** اضافه شده است.

### ۲. 📍 تحلیل عمیق پروفایل (Deep Forensics)
در بالای پروفایل هر کاربر، کادری ظاهر می‌شود که شامل موارد زیر است:
*   **موقعیت (Location):** کشور واقعی ثبت شده برای اکانت.
*   **دستگاه (Device):** نوع گوشی (آیفون/اندروید) و منطقه (Region) اپ‌استور.
*   **شناسه ثابت (Perm ID):** عدد یکتایی که حتی با تغییر نام کاربری (`@handle`) تغییر نمی‌کند.
*   **تاریخ دقیق:** زمان ساخت اکانت با دقت ثانیه.
*   **تغییر نام:** تعداد دفعاتی که کاربر نام کاربری خود را عوض کرده است.

### ۳. 🧠 تشخیص هوشمند ناهنجاری (Smart Anomaly Detection)
این ابزار با تحلیل همزمان **زبان، لوکیشن و نوع اتصال**، اکانت‌های مشکوک را شناسایی می‌کند:
*   **🛡️ سپر فعال (Shield):** کاربر از **VPN** یا پروکسی استفاده می‌کند.
*   **✅ اتصال مستقیم (High Confidence):** کاربر بدون واسطه وصل شده است.
*   **⚠️ ناهنجاری (Hidden Identity):**
    *   اگر کاربری **فارسی** صحبت کند (در بیو یا توییت‌ها) + لوکیشن او **«غرب آسیا (West Asia)»** یا **«ایران»** باشد + اتصالش **«مستقیم»** باشد:
    *   ابزار هشدار می‌دهد که این کاربر احتمالا از **سیم‌کارت سفید (White SIM)**، **اینترنت طبقاتی** یا **سرورهای تانلینگ داخلی** استفاده می‌کند.

### ۴. 📂 اکسپلورر کاربران (User Database Dashboard)
تمام کاربرانی که در تایم‌لاین یا پروفایل می‌بینید، در دیتابیس مرورگر شما ذخیره می‌شوند. با باز کردن داشبورد می‌توانید:
*   **لیست کامل:** تمام کاربرانی که تا به حال رصد کرده‌اید را ببینید.
*   **جستجو و فیلتر:** مثلاً فقط کاربرانی که لوکیشن «ایران» دارند یا ریسک «Anomaly» دارند را فیلتر کنید.
*   **صفحه‌بندی (Pagination):** حتی اگر ۵۰,۰۰۰ کاربر ذخیره کرده باشید، داشبورد بدون کندی کار می‌کند.

### ۵. ☁️ همگام‌سازی و مشارکت (Cloud & Contribution)
*   **Update from GitHub:** دیتابیس خود را با دیتابیس جهانی (شامل هزاران کاربر شناسایی شده) ترکیب کنید. (تکراری‌ها حذف می‌شوند).
*   **Contribute Data:** اگر لیست بزرگی از اکانت‌های مشکوک جمع‌آوری کردید، می‌توانید با یک کلیک فایل جیسون را دریافت کرده و برای کمک به جامعه ارسال کنید.

---

## 🚀 راهنمای نصب (Installation Guide)

### 💻 روش نصب در کامپیوتر (Windows / Mac)
1.  روی مرورگر خود (Chrome, Edge, Firefox, Brave) افزونه **Tampermonkey** را نصب کنید.
2.  روی آیکون افزونه کلیک کرده و **Create a new script** را بزنید.
3.  کدهای قبلی را پاک کرده و کد جدید `x-forensics.js` را کپی و پیست کنید.
4.  از منوی File گزینه **Save** را بزنید.
5.  وارد `x.com` شوید. گزینه Forensics در منوی سمت چپ ظاهر می‌شود.

### 🤖 روش نصب در اندروید (Android)
1.  مرورگر **Kiwi Browser** یا **Firefox Nightly** را دانلود کنید.
2.  در مرورگر، افزونه Tampermonkey را نصب کنید.
3.  اسکریپت را مانند روش کامپیوتر اضافه کنید.
4.  با مرورگر وارد توییتر شوید. آیکون فولدر در نوار پایین صفحه ظاهر می‌شود.

### 🍎 روش نصب در آیفون (iOS)
1.  برنامه رایگان **Userscripts** را از اپ‌استور نصب کنید.
2.  در تنظیمات آیفون: `Settings > Apps > Safari > Extensions`، گزینه Userscripts را فعال کنید.
3.  در برنامه Userscripts، یک پوشه را انتخاب کنید (Set Directory).
4.  یک فایل متنی (مثلا `script.js`) در آن پوشه بسازید و کد را در آن ذخیره کنید.
5.  در سافاری وارد توییتر شوید و روی علامت **آأ (aA)** در نوار آدرس بزنید تا افزونه فعال شود.

---

### ⚠️ درباره محدودیت‌های توییتر (Rate Limits)

توییتر (X) برای جلوگیری از فعالیت ربات‌ها، تعداد دفعاتی که می‌توانید اطلاعات پروفایل‌ها را در یک بازه زمانی کوتاه چک کنید، محدود می‌کند. به این قانون **Rate Limit** می‌گویند.

**این ابزار چگونه شما را محافظت می‌کند؟**
برای اینکه اکانت شما به محدودیت نخورد، ما از ۳ تکنیک هوشمند استفاده کرده‌ایم:

1.  **لود تنبل (Lazy Loading):**
    در تایم‌لاین، اطلاعات کاربران به صورت خودکار دانلود نمی‌شود. آیکون هدف (⌖) نمایش داده می‌شود و **فقط زمانی که موس را روی آن ببرید (یا کلیک کنید)**، اطلاعات آن کاربر خاص دریافت می‌شود. این کار باعث می‌شود هزاران درخواست بیهوده ارسال نشود.

2.  **سیستم کش (Smart Caching):**
    وقتی یک‌بار کاربری را چک کردید، اطلاعات او در دیتابیس مرورگر شما ذخیره می‌شود. دفعات بعدی، بدون ارسال درخواست به توییتر، اطلاعات از حافظه خوانده می‌شود.

3.  **مدیریت خطا (Error Handling):**
    اگر بیش از حد (مثلاً ۵۰ کاربر در دقیقه) چک کنید، ممکن است با خطای **"Rate Limit Exceeded"** یا کد **429** مواجه شوید.
    *   **نگران نباشید:** این یک بن دائمی نیست.
    *   **راه‌حل:** فقط کافیست ۱۵ دقیقه صبر کنید تا محدودیت برداشته شود. اکانت شما امن است.

---

## 💡 راهنمای وضعیت‌ها (Status Indicators)

| وضعیت | رنگ | معنی |
| :--- | :---: | :--- |
| **High Confidence** | 🟢 سبز | اتصال مستقیم و طبیعی است. |
| **Shield Active (Normal)** | 🟢 سبز | کاربر ایرانی با VPN. (رفتار طبیعی برای کاربران عادی). |
| **Anomaly Detected** | 🟠 نارنجی | لوکیشن ایران/غرب آسیا با اتصال مستقیم (مشکوک به نت دولتی/سیم‌کارت خاص). |
| **Shield Active** | 🔴 قرمز | کاربر خارجی با هویت مخفی شده (VPN). |
| **Hidden Identity** | 🔴 قرمز | فارسی زبان در غرب آسیا با اتصال مستقیم (ریسک بالا). |

---

## ❓ سوالات متداول

**دیتابیس من کجاست؟**
اطلاعات فقط در حافظه مرورگر شما (Local Storage) ذخیره می‌شود و هیچ‌جا آپلود نمی‌شود مگر اینکه خودتان دکمه Contribute را بزنید.

**چرا دکمه Refresh اضافه شده؟**
گاهی اوقات توییتر اطلاعات را کش می‌کند. اگر دیتای کاربری قدیمی بود یا لود نشد، دکمه `↻` را داخل کارت پروفایل بزنید تا اطلاعات مستقیم از سرور گرفته شود.

**آیا خطر مسدودی اکانت وجود دارد؟**
خیر. این ابزار فقط درخواست‌های "خواندن" (Read-only) ارسال می‌کند و از سیستم Lazy Loading استفاده می‌کند تا محدودیت‌های توییتر (Rate Limit) را نقض نکند.

---
**توسعه داده شده برای شفافیت.**
