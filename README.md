# X Profile Forensics (ابزار جرم‌شناسی پروفایل ایکس)

این اسکریپت یک ابزار پیشرفته **OSINT** (جستجوی اطلاعات آشکار) برای شبکه اجتماعی **ایکس (توییتر سابق)** است. این ابزار اطلاعات مخفی و فنی حساب‌های کاربری را که در حالت عادی قابل مشاهده نیستند، استخراج کرده و در هدر پروفایل نمایش می‌دهد.

این اسکریپت به طور کامل ریسپانسیو بوده و روی **کامپیوتر (ویندوز/مک)** و **موبایل (اندروید/iOS)** کار می‌کند.

---

## ✨ ویژگی‌ها و امکانات (Features)

این ابزار با تحلیل پاسخ‌های JSON سرورهای ایکس، اطلاعات زیر را نمایش می‌دهد:

### 📍 ۱. موقعیت مکانی و دستگاه (Location & Device)
*   **کشور (Account Based In):** نمایش کشوری که اکانت در آن ثبت شده است (بر اساس IP یا شماره تلفن).
*   **نوع دستگاه و ریجن:** تشخیص اینکه کاربر از **آیفون (iPhone)** استفاده می‌کند یا **اندروید (Android)** و اینکه اپلیکیشن توییتر را از اپ‌استورِ کدام کشور (مثلاً آمریکا، انگلستان، ایران) دانلود کرده است.

### 🛡️ ۲. وضعیت اتصال و امنیت (Shield & Connection Logic)
این ابزار وضعیت اتصال کاربر به اینترنت را تحلیل می‌کند:
*   **🛡️ سپر فعال (Shield Active):** اگر کاربر از **VPN، پروکسی** یا **Tor** استفاده کند، توییتر آن را تشخیص داده و اسکریپت هشدار می‌دهد.
*   **✅ اتصال مستقیم (High Confidence):** اگر اتصال کاربر بدون واسطه تشخیص داده شود (Direct Access).
*   **⚠️ تشخیص ناهنجاری (Anomaly - مخصوص ایران):**
    *   اگر لوکیشن کاربر **"ایران"** باشد اما توییتر اتصال را **"مستقیم/دقیق"** (Accurate) تشخیص دهد، اسکریپت هشدار می‌دهد.
    *   **توضیح:** از آنجا که توییتر در ایران فیلتر است، اتصال مستقیمِ واقعی از طریق IP غیرممکن است. این حالت معمولاً زمانی رخ می‌دهد که کاربر با **شماره موبایل داخلی (+98)** ثبت‌نام کرده باشد، از **سیم‌کارت‌های خاص (سفید)** استفاده کند یا از روش‌های **Serverless/Tunneling** استفاده نماید که IP ایران را لو می‌دهد.

### 🕵️ ۳. اطلاعات جرم‌شناسی (Forensics)
*   **🆔 شناسه ثابت (Permanent REST ID):** نام کاربری (`@username`) قابل تغییر است، اما این شناسه عددی **هرگز** تغییر نمی‌کند. مناسب برای ردیابی کاربرانی که مدام نام خود را عوض می‌کنند.
*   **📅 زمان دقیق ساخت:** نمایش تاریخ و **ساعت/دقیقه/ثانیه** دقیق ساخت اکانت.
*   **🔄 تاریخچه تغییر نام:** اگر کاربر اخیراً نام کاربری خود را تغییر داده باشد، تعداد دفعات آن نمایش داده می‌شود.
*   **🪪 وضعیت احراز هویت:** تشخیص اینکه آیا کاربر کارت ملی/پاسپورت خود را برای توییتر ارسال کرده است یا خیر (Government ID Verified).

### 🔎 ۴. دانلود آواتار
*   لینک مستقیم برای مشاهده و دانلود عکس پروفایل با **کیفیت اصلی (Original Size)** جهت بررسی‌های بیشتر.

---

## 🚀 راهنمای نصب (Installation)

### 💻 ۱. نصب روی ویندوز / مک (Desktop)
برای استفاده در مرورگرهای کروم، فایرفاکس، اج یا بریو:

1.  افزونه **Tampermonkey** را روی مرورگر خود نصب کنید.
    *   [دانلود برای Chrome/Edge](https://chrome.google.com/webstore/detail/tampermonkey/dhdgffkkebhmkfjojejmpbldmpobfkfo)
    *   [دانلود برای Firefox](https://addons.mozilla.org/en-US/firefox/addon/tampermonkey/)
2.  روی آیکون Tampermonkey کلیک کرده و گزینه **Create a new script** را بزنید.
3.  کدهای موجود را پاک کنید و کد اسکریپت `X Profile Forensics` را کامل جایگذاری کنید.
4.  دکمه **File > Save** را بزنید.
5.  وارد سایت `x.com` شوید.

### 🤖 ۲. نصب روی اندروید (Android)
از آنجا که اپلیکیشن رسمی ایکس از اسکریپت پشتیبانی نمی‌کند، باید از مرورگرهایی استفاده کنید که افزونه خور هستند:

1.  مرورگر **Kiwi Browser** یا **Firefox Beta** را از گوگل پلی نصب کنید.
2.  در مرورگر Kiwi، وارد بخش Extensions شده و **Tampermonkey** را نصب کنید.
3.  مانند روش ویندوز، یک اسکریپت جدید بسازید و کد را در آن کپی کنید.
4.  با مرورگر Kiwi وارد `x.com` شوید.

### 🍎 ۳. نصب روی آیفون (iOS - iPhone/iPad)
برای اجرا در مرورگر سافاری (Safari):

1.  برنامه رایگان و متن‌باز **Userscripts** را از اپ استور دانلود کنید.
2.  به تنظیمات آیفون بروید: `Settings > Apps > Safari > Extensions`.
3.  گزینه **Userscripts** را فعال کنید و دسترسی آن را روی **Allow** قرار دهید.
4.  برنامه Userscripts را باز کنید و پوشه‌ای را برای ذخیره اسکریپت‌ها انتخاب کنید.
5.  در آن پوشه (در فایل‌های آیفون)، یک فایل متنی بسازید (مثلا `x-forensics.js`) و کد را داخل آن پیست کنید.
6.  سافاری را باز کرده و وارد `x.com` شوید.

---

## 📖 راهنمای استفاده (Usage)

پس از نصب، وارد پروفایل هر کاربری در ایکس شوید. در بالای صفحه (کنار تاریخ عضویت)، اطلاعات جدیدی اضافه خواهد شد:

1.  **مشاهده سریع:**
    *   اطلاعاتی مثل `📱 iPhone` یا `📍 Iran` را مستقیماً در هدر می‌بینید.
2.  **مشاهده جزئیات کامل:**
    *   **در کامپیوتر:** موس را روی آیکون وضعیت (🛡️ یا ✅ یا ⚠️) نگه دارید تا پنجره جزئیات باز شود.
    *   **در موبایل:** روی آیکون وضعیت **ضربه بزنید** (Tap) تا پنجره جزئیات در وسط صفحه باز شود.

---

## 💡 راهنمای آیکون‌ها (Status Icons)

| آیکون | معنی | توضیحات |
| :---: | :--- | :--- |
| 🛡️ | **هشدار (Shield Active)** | کاربر از VPN، پروکسی یا Tor استفاده می‌کند. موقعیت مکانی دقیق نیست. |
| ✅ | **تایید شده (Verified/Direct)** | اتصال مستقیم است. کاربر احتمالا از شماره لوکال یا اینترنت بدون فیلتر استفاده می‌کند. |
| ⚠️ | **ناهنجاری (Anomaly)** | **مخصوص ایران:** لوکیشن ایران است اما توییتر اتصال را "مستقیم" تشخیص داده. احتمال استفاده از شماره مجازی، سیم‌کارت خاص یا تانلینگ. |
| 🟢 | **ریسک پایین** | کاربر هویت تایید شده (کارت ملی) دارد و اطلاعاتش همخوانی دارد. |
| 🔴 | **ریسک بالا** | اکانت تازه ساخت، با تغییر نام زیاد و استفاده از VPN. |

---

### سلب مسئولیت (Disclaimer)
این ابزار صرفاً جهت اهداف آموزشی و تحقیقاتی (OSINT) توسعه یافته است. تمام اطلاعات نمایش داده شده از داده‌های عمومی (Public API) خود شبکه اجتماعی ایکس استخراج می‌شود و هیچ‌گونه هکی صورت نمی‌گیرد.
